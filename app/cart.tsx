import { View } from 'react-native';
import React from 'react';
import CartScreen from './features/screens/Cart';

const Cart = () => {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <CartScreen />
    </View>
  );
};

export default Cart;
